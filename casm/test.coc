// Declare varibles before main.casm
`var foo 1`
`inc "casm/main.casm"`

`; Test not equal`
equ (1, 1) {
	print('H');
}

`; Assignment to register before function call`
ga = 'i';
print();

`; Store 1 in ga and 2 in gb`
(1, 2);
1;2;

`; Call function again instead of resetting value`
print('!') print();

